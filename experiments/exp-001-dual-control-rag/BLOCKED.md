# BLOCKED — provider API path

**Status:** The OpenAI API path was declined by the user. No OpenAI API key was requested or used.

## Path used instead

This experiment was completed with the Grok Bot `local-agent` path. The exact 15 parent-decided decisions are recorded in `grok_bot_decisions.json`; retrieval uses the local harness and assertions use `assert_policy.evaluate_run`.

**Provider:** `grok-bot`  
**Model id:** `grok-bot`  
**API mode:** `local-agent`  
**External provider calls:** none

The previous mock pipeline artifact is archived at `results/summary_mock.md`. The active result is `results/summary.md`.
